[project]
name = "persistent-repetition-of-phrases"
version = "0.1.0"
description = "A collection of programming exercises and practice"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"